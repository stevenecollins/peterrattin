<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<title>{{ title }}</title>
		<link rel="stylesheet" href="{{ '../scss/styles.css' | url }}" />
	</head>
	<body id="{{ pageId }}">
		{% include 'header.njk' %}
		<main>
			{{ content | safe }}
		</main>
		{% include 'footer.njk' %}
		<button class="scrollToTopBtn scroll"></button>
		<script src="{{ '../js/portfolio.js' | url }}"></script>
	</body>
</html>
